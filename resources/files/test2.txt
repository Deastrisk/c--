char: operator;
double: num1;
double: num2;
double: result;
char: choice;

# Set choice to 'y' to start the loop
choice = 'y';

while (choice == 'y') {
    echo("Welcome to the Simple Calculator!\n");
    echo("---------------------------------\n");

    # Ask the user for the operator
    echo("Enter an operator (+, -, *, /): ");
    # (Input logic for operator would go here)

    # Ask the user for two numbers
    echo("Enter two numbers separated by a space: ");
    # (Input logic for num1 and num2 would go here)

    # Perform the calculation based on the operator using arrow syntax
    switch (operator) {
        case '+' -> {
            result = num1 + num2;
            echo("Result: %.2lf + %.2lf = %.2lf\n");
        }

        case '-' -> {
            result = num1 - num2;
            echo("Result: %.2lf - %.2lf = %.2lf\n");
        }

        case '*' -> {
            result = num1 * num2;
            echo("Result: %.2lf * %.2lf = %.2lf\n");
        }

        case '/' -> {
            # Check for division by zero
            if (num2 != 0.0) {
                result = num1 / num2;
                echo("Result: %.2lf / %.2lf = %.2lf\n");
            } else {
                echo("Error! Division by zero is not allowed.\n");
            }
        }

        default ->
            echo("Error! Invalid operator entered.\n");
    }

    # A simple for loop to simulate a visual separator / progress bar
    echo("Processing visual separator: ");
    for (int: i = 0; i < 5; i = i + 1) {
        echo(". ");
    }
    echo("\n");

    # Ask if the user wants to continue
    echo("Do you want to perform another calculation? (y/n): ");
    # (Input logic to update choice variable goes here)
}

echo("Thank you for using the calculator! Goodbye.\n");